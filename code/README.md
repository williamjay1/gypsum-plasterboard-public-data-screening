# Analysis scripts

The scripts in this directory use paths relative to the repository root. Run them from the repository root or execute them by their full path.

The main figure script is `project23_make_et_redesign_figures.py`. The remaining scripts generate the public electricity sensitivity, boundary sensitivity, failure mode and robust quality gate tables.

The calculations are deterministic public data analyses. They do not collect samples, run laboratory experiments or use participant data.

